import { Invalid_argument } from '@bithana/general-exception/build/internal/caller_fault/invalid_argument';
export declare class Invalid_platform extends Invalid_argument {
    constructor(platform: string);
}
