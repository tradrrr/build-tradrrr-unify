export declare const api_config: {
    coinbase_pro: {
        http: {
            protocol: string;
            domain: string;
        };
        ws: {
            protocol: string;
            domain: string;
        };
    };
};
